example.com/Customer/*/Purchases

Customers is a query
purchases is a query

example.com/Customers/*/Purchases?Customers.query="Active: == true"&Purchases.query="CreateDate: > 10/24/22"

example.com/Customers/*/Purchases?Customers.query=Active,Name,Zip:exclude(contains(55555))

example.com/Customers/*/Purchases?Customers.query=Active,Name,Zip:contains(55555)


required becomes default fields
dyre must have at least one default field

if a query is part of a sub query should it still generate?

INNER JOIN is implicit


SELECT PurchaseID 
FROM Purchases
INNER JOIN (
  SELECT Customrs.Active
  FROM Customers
  WHERE Customers.Active == true
) AS Customers ON Customer.CustomerID = Purchases.CustomerID
WHERE Purchases.CreateDate > GetDate()

c = dyre['Customers']
c.query = Customers.query

p = dyre['Purchases']
p.query = Purchases.Query

p.join(c, CustomerID)
p.group = Purchases.Group

query := p.generate()

ident: logic expression. data type methods 

conditions on return

Column creation?


@(CustomerID) > 5;

ColumnFunctions

AS, GROUP, MAX, MIN, COUNT 

CustomerID:@('year', datepart(year,@(CreateDate))): == '2025'

datepart('year', @('CreateDate')) > '2025';

AS('year', value_expression ): AS

if contains alias with where expression

SELECT Customers.CustomerID, ( Customers.Active ) as isActive
FROM Customers
WHERE Customers


{ 
 Type
 Endpoint: {
  Query:
  Joins: []
 }
}


{
 Query:
 Endpoint:
 Joins: []
 GroupBy
} 

{
 Query:
 Endpoint:
 Joins: [
  {
   Type: "INNER",
   Query: ""
   Endpoint: ""
   Joins: []
  }
 ]
} 









